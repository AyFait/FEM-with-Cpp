#include <iostream> // Include the iostream library for input/output operations

// The main function (an integer) is the entry point of the program
int main(){             
    std::cout << "Hello, World!" << std::endl; // Output "Hello, World!" to the console followed by a newline
    return 0;
}

//Basic number types in C++ include:
//1. Integer types: int, short, long, long long 
//2. Unsigned int: Non-negative integer types
//3. Double: real numbers with double precision (64-bit)
//4. Floating-point types: float, double, long double (like double but 32-bit precision)


int main2() {
        int a;
        int b, c;
        int d = 1, e = 3, f;

        float g = 3.14f; // 'f' suffix indicates a float literal
        double h = 2.71828; // double precision floating-point literal
        
        return 0;


}